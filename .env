# === NEXTVISION CONFIGURATION ===
ENVIRONMENT=development
DEBUG=true
API_VERSION=1.0.0

# === COMMITMENT- SERVICES URLS ===
# Job Parser GPT
COMMITMENT_JOB_PARSER_PRIMARY=http://localhost:5053/api/parse-job
COMMITMENT_JOB_PARSER_SECONDARY=http://localhost:7000/api/job-parser
COMMITMENT_JOB_PARSER_PRODUCTION=/api/job-parser

# CV Parser GPT 
COMMITMENT_CV_PARSER_PRIMARY=http://localhost:5055/api/parse-cv

# === BRIDGE CONFIGURATION ===
BRIDGE_REQUEST_TIMEOUT=30
BRIDGE_DEBUG=true
BRIDGE_AUTO_DETECT_SERVICES=true

# === NEXTVISION MATCHING ===
NEXTVISION_HOST=0.0.0.0
NEXTVISION_PORT=8000
NEXTVISION_RELOAD=true

# === ADAPTIVE WEIGHTING ===
ADAPTIVE_WEIGHTING_ENABLED=true
DEFAULT_CONFIDENCE_THRESHOLD=0.7

# === LOGGING ===
LOG_LEVEL=INFO
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s
